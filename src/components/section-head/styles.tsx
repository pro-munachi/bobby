import styled from "styled-components";

export const Section = styled.div`
    font-style: normal;
  font-weight: normal;

  .main {
    font-size: 28px;
    line-height: 35px;
    margin-bottom: 9px;
    color: $primary;
  }

  .sub {
    font-size: 16px;
    line-height: 20px;
    color: $accent-grey;
  }
`;
